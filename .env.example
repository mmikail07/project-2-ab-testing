# Copy to .env and fill in if you need to override defaults. Never commit .env.
# Project 2 has no required secrets — all paths and seeds resolve via src/config.py.
# Provided for parity with Project 1 and future extension.

# Optional: override the experiment seed for ad-hoc reruns
# EXPERIMENT_SEED=42

# Optional: override the processed-data path
# DATA_PROCESSED_DIR=
